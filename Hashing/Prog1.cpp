#include <iostream>
using namespace std;
int main(){
    int arr[];
    int n;
    cout<<"Enter the number of values in array:";
    cin>>n;
    cout<<"Enter values:";
    for(int i=0; i<n; i++){
        cin>>a[i];
    }

    int hash[]={0};
    if hash[arr[i]]==number;
    hash[arr[i]]++;
    int q;
    cout<<"Enter the number of searches:";
    cin>>q;
    cin>>number;
    cout<<"Enter the values to be searched:";
    while(q--){
        cin>>number;
    }   

}